<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>FeedTag</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
  	
  	<div class="menu contain-to-grid">
      <nav class="menu top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
            <h1><%= link_to "FeedTag", root_path %></h1>
          </li>
          <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
          <% if current_user %>
            Hola! <%= current_user.nombre %>!
            <%= link_to "Salir", signout_path %>
          <% else %>
            <%= link_to "Ingresa con Twitter", "/auth/twitter" %>
          <% end %>
        </ul>
        <section class="top-bar-section">
          <ul class="right">
            <li>
              <%= link_to "Inicio", root_path %>
            </li>
          </ul>
        </section>
      </nav>
    </div>
    
    <section role="main">
      <%= yield %>
    </section>
    <%= javascript_include_tag "application" %>
  </body>
</html>